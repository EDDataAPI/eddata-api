# EDData API - Environment Configuration Example
# Copy this file to .env and adjust the values to your needs

# ==========================================
# API Configuration
# ==========================================

# Base URL of the API (used for generating links and redirects)
EDDATA_API_BASE_URL=http://localhost:3001/api

# Port on which the API server will listen
EDDATA_API_LOCAL_PORT=3001

# ==========================================
# Data Directories
# ==========================================

# Main data directory (contains SQLite databases)
# Default: ./eddata-data (development) or /app/data (container)
EDDATA_DATA_DIR=/app/data

# Cache directory for JSON files and temporary data
# Default: ${EDDATA_DATA_DIR}/cache
EDDATA_CACHE_DIR=/app/data/cache

# Backup directory for database backups
# Default: ./eddata-backup (development) or /app/backups (container)
EDDATA_BACKUP_DIR=/app/backups

# Downloads directory for downloaded files
# Default: ./eddata-downloads (development) or /app/downloads (container)
EDDATA_DOWNLOADS_DIR=/app/downloads

# ==========================================
# Domain Configuration
# ==========================================

# Domain name for the API (used in headers and responses)
EDDATA_DOMAIN=eddata.app

# ==========================================
# Node.js Environment
# ==========================================

# Set to 'production' for production deployment
# Set to 'development' for local development
NODE_ENV=production

# ==========================================
# Database Paths (Advanced)
# ==========================================
# Usually these are automatically derived from EDDATA_DATA_DIR
# Only set if you need custom database locations

# EDDATA_SYSTEMS_DB=${EDDATA_DATA_DIR}/systems.db
# EDDATA_LOCATIONS_DB=${EDDATA_DATA_DIR}/locations.db
# EDDATA_STATIONS_DB=${EDDATA_DATA_DIR}/stations.db
# EDDATA_TRADE_DB=${EDDATA_DATA_DIR}/trade.db

# ==========================================
# Cache Configuration (Advanced)
# ==========================================
# Cache file paths - usually automatically derived from EDDATA_CACHE_DIR

# EDDATA_MARKET_TICKER_CACHE=${EDDATA_CACHE_DIR}/commodity-ticker.json
# EDDATA_GALNET_NEWS_CACHE=${EDDATA_CACHE_DIR}/galnet-news.json

# ==========================================
# Notes
# ==========================================
# - In Docker/Container environments, paths default to /app/* locations
# - In development, paths default to ./eddata-* directories relative to project root
# - All directories will be created automatically if they don't exist
# - SQLite databases must be placed in EDDATA_DATA_DIR
# - Cache files are regenerated automatically via cron tasks
